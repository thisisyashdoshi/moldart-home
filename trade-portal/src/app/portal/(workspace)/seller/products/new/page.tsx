import { SectionCard } from '@/components/portal/workspace-shell';

export default function SellerNewProductPage() {
  return (
    <SectionCard title="New product draft" subtitle="Product creation UI placeholder wired for future server mutations">
      <p className="text-sm leading-7 text-slate-600">Phase 2 will extend this page with seller product create/update forms, attribute editing, and document upload. The repository and schema are already prepared for seller-scoped product ownership.</p>
    </SectionCard>
  );
}
