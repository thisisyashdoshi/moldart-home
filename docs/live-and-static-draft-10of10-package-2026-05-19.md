# Moldart Live And Static Draft 10/10 Package

Generated for no-deploy review. This package does not change production, DNS, Cloudflare, GitHub, or the frozen `existing/` reference.

## Current Drafts

- Live deployed site: `https://moldartindia.com/`, currently a Next.js-style static export on production.
- Local static draft: `http://127.0.0.1:4173/` when `preview-server.js` is running from this workspace.
- Static artifact draft: `public-site/`, regenerated by `npm run build`.

## What Is Already Complete Locally

- 51 published insight pages are generated in the static draft.
- Each published insight has one editorial/internal image.
- Each published insight renders one selected outbound Moldart YouTube video card.
- YouTube library is existing-insight-only: 40 public videos, 18 long-form, 22 Shorts, no references to rejected new insight slugs.
- `llms.txt` and `llms-full.txt` exist in the draft and use Markdown links with canonical identity details.
- Product hub `/products/` exists as a real page and is in the sitemap.
- Draft headers are defined in `_headers` for HSTS, CSP, frame protection, content-type protection, referrer policy, permissions policy, COOP/CORP, and long-lived static asset caching.
- `/goal` and `/automation-status` project commands were added to `opencode.jsonc`; restart opencode before using them.
- FOSS/self-hosted monitoring plan exists in `docs/self-hosted-automation-plan.md`.

## Why The Live Site Needs A Draft Upgrade Instead Of Direct Editing

- The active `TECH/WEBSITE/existing-new/work` tree does not contain the deployed Next.js public-source app.
- The closest live match found was a static publish export under `C:/Users/Yash/Documents/Codex/2026-05-06/i-want-you-to-aggressively-check/moldart-home-clean-deploy-publish`.
- Editing a published export directly is brittle because the next real build can overwrite it.
- The safe path is to treat the current static draft in this workspace as the 10/10 upgrade base, test it as a Cloudflare Pages preview later, and deploy only after explicit approval.

## Live Site Gaps Closed By The Static Draft

- Live has 61 sitemap URLs; draft has 83 sitemap URLs.
- Live has 31 insight guide URLs; draft has 51 public insight decision sheets plus the index.
- Live `/llms-full.txt` is missing; draft has stable `200` responses for both `llms.txt` and `llms-full.txt`.
- Live sitemap/canonicals were observed using `www` while runtime redirects to apex; draft uses apex canonicals and sitemap URLs.
- Live checked pages lacked Open Graph/Twitter card metadata; draft includes social metadata.
- Live insights do not render YouTube references; draft insight pages render one selected outbound YouTube card each.
- Live contact form is a no-required-fields GET form; draft contact forms use POST plus required buyer fields and autocomplete.
- Live headers sampled from production lacked visible HSTS, CSP, and X-Frame-Options; draft artifact includes them in `_headers`.
- Live homepage showed Next/RSC redirect and preload warnings; draft browser checks were clean in local QA.

## 10/10 Release Criteria

- `npm run build` passes.
- `npm run goal` passes.
- `node scripts/moldart-quality-monitor.mjs --base=http://127.0.0.1:4173` passes while local preview is running.
- `npm run audit:site -- http://127.0.0.1:4173` passes while local preview is running.
- `npm run compare:live-draft` writes a clean comparison report or expected live-only gaps.
- `npm run preview:ensure` verifies the local preview link or starts a local preview process before sharing a draft URL.
- `npm run release:check` validates the generated `public-site/` artifact before any Cloudflare preview or production release.
- Browser smoke checks pass on home, products, resources, insights, representative insight pages, and contact.
- Lighthouse local desktop and mobile stay at 100 Accessibility, 100 Best Practices, 100 SEO, 100 Agentic Browsing.
- Cloudflare Pages preview later proves `_headers`, `_redirects`, compression, cache policy, sitemap, `llms.txt`, `llms-full.txt`, PDFs, and forms before production deployment.

## Remaining Decision Before Deployment

- Confirm whether the static draft should replace the current live Next.js-style export as the public website source of truth.
- Confirm whether FormSubmit is acceptable for RFQ/resource forms or whether these should move to a controlled serverless endpoint.
- Confirm long-term hosting for six oversized PDF downloads currently served through the existing public-downloads route.
- Confirm favicon/manifest brand mark preference before final production release.

## No-Deployment Rule

No deployment should happen from this package. Production release needs a separate explicit approval and a final preview-stage QA run.
